# AGENTS.md — Agent Safety Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

This file defines safety constraints for any AI agent working in **contract-risk-review-pipeline**.

## Project

CUAD contract review pipeline: extract labeled clauses, compare to a playbook, flag risk, write an audit log. CLAUDE.md already exists — do not overwrite it unless the owner asks.

## Permitted Behaviors

- Read `main.py`, `src/`, `tests/`, `data/playbook.json`, and the existing `CLAUDE.md`.
- Run `pytest tests/ -v`.

## Prohibited Behaviors

| Prohibition | Reason |
|------------|--------|
| Print, log, or surface secret/env values | Credential leakage |
| Auto-commit or auto-push | Needs human review |
| Delete files without explicit confirmation | Irreversible |
| Modify `.git/` directly | Corrupts history |
| Install packages globally | Pollutes system Python |
| Run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`) | Data loss |

## Project-specific rules

- Do not replace the CUAD ground-truth workflow with free-form LLM legal advice.
- Do not upload real client contracts to third-party APIs unless the owner explicitly asks.
- Keep `data/` fixtures as sample/legal-research data, not production client files.

## Blocked paths

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Escalation

If you find a committed `.env`, private key, or live credential: **stop**, report the filename only (never the value), and wait for a human.

## Verification

```bash
pytest tests/ -v
```
